SELECT A.TITLE, A.BOARD_ID, B.REPLY_ID, B.WRITER_ID, B.CONTENTS, DATE_FORMAT(B.CREATED_DATE, '%Y-%m-%d') FROM (SELECT * FROM USED_GOODS_BOARD WHERE CREATED_DATE LIKE '2022-10%') A INNER JOIN USED_GOODS_REPLY B ON A.BOARD_ID=B.BOARD_ID ORDER BY B.CREATED_DATE, A.TITLE;